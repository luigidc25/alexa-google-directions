#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@google/maps/bin/run.js" "$@"
  ret=$?
else 
  node  "$basedir/../@google/maps/bin/run.js" "$@"
  ret=$?
fi
exit $ret
